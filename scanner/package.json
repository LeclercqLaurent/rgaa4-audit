{
  "name": "rgaa-scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Service de scan a11y (Puppeteer + axe-core). Entrée = URL, sortie = JSON axe brut sur stdout.",
  "type": "commonjs",
  "license": "MIT",
  "scripts": {
    "scan": "node scan.js"
  },
  "dependencies": {
    "axe-core": "^4.10.2",
    "puppeteer": "^24.10.0"
  }
}
